export const validators = {
    // eslint-disable-next-line
    name: /^[A-Za-z\.]{2,50}$/,
    // eslint-disable-next-line
    street: /^[A-Za-z\.]{2,50}$/,
    postal: /^\d{5}$/,
    // eslint-disable-next-line
    city: /^[A-Za-z\.]{2,50}$/,
}